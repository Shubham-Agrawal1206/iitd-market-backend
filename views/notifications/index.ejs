<%- include ("../partials/header") %>

<h1>Notifications</h1>

<ul>
	<% allNotifications.forEach(function(notification) { %>
	<li>
		<a href="/course/<%= notification.courseId %>">
			<%= notification.username %> <%= notification.message%>
		</a>
	</li>
	<% }); %>
</ul>

<%- include ("../partials/footer") %>